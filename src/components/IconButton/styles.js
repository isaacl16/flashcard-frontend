import styled from "styled-components/macro";

export const StyledIcon = styled.img`
    width: 32px;
    height: auto;
    color: black;
    cursor: pointer;
    transition: 0.2s;
    :hover {
        transform: scale(1.2);
    }
`